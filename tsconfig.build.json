{
  "extends": "./tsconfig.base.json",
  "//": "Declaration build only, consumed by vite-plugin-dts. Excludes tests/stories/mocks from the emitted output (mirrors the `files` exclusions in package.json and the exclusion regex in vite.config.mts). Linting and typechecking use tsconfig.json, which keeps these files in the project.",
  "exclude": [
    "node_modules",
    "out",
    "src/**/tests/**",
    "src/**/stories/**",
    "src/**/__mocks__/**",
    "src/**/__tests__/**",
    "src/**/*.test.*",
    "src/**/*.spec.*",
    "src/**/*.jest.*",
    "src/**/*.playwright.*",
    "src/**/*.puppeteer.*",
    "src/**/*-mock.*",
    "src/setupTests.js"
  ]
}
